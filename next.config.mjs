/** @type {import('next').NextConfig} */
const nextConfig = {
  // Below images onfiguration is required to use dynamic image URL
  images: {
    domains: ["i.ibb.co"],
  },
};

export default nextConfig;
